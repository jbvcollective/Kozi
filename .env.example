# Copy to .env and fill in. Never commit .env or real secrets.
# OWASP: Do not use NEXT_PUBLIC_* for any secret. Rotate keys if exposed.

# --- Required (Express server) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Optional: API key for server-to-server calls to /api. Never expose to client. ---
# API_KEY=

# --- CORS / URLs ---
# CORS_ORIGIN=http://localhost:3001
# FRONTEND_URL=http://localhost:3001

# --- Server tuning ---
# NODE_ENV=development
# PORT=3000
# BODY_LIMIT=256kb
# REQUEST_TIMEOUT_MS=30000
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_GLOBAL=200
# RATE_LIMIT_MAX_STRICT=60

# --- Secrets (server-only scripts / services). Never commit. ---
# GEMINI_API_KEY=
# GOOGLE_PLACES_API_KEY=   or GOOGLE_MAPS_API_KEY=
# PROPTX_API_KEY=          (fetch scripts)
# PROPTX_BASE_URL=         (fetch scripts)

# --- Scripts ---
# UPSERT_BATCH_SIZE=500
# SYNC_SOLD_BATCH_SIZE=500
# GOOGLE_PLACES_CENTERS=   (JSON array for syncGooglePlaces.js)
# GEOCODING_API_KEY=      (Google Geocoding API key for geocodeListingsMissingCoords.js – map pinpoints)
